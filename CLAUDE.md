# CLAUDE.md — 건강보험노트 (health-note)

건강보험에서 **실제로 돈이 갈리는 지점** — 피부양자 자격·본인부담상한제 환급·
건강검진 대상·병원비 본인부담률을 계산기 4종과 가이드 10편으로 정리하는
애드센스 수익형 미니사이트. 생활반장 노트 시리즈의 17번째 노트.

> 경계 주의: **퇴사노트**가 임의계속가입을, **급여노트**가 직장가입자 4대보험을
> 다룬다. 이 노트는 그 둘을 다시 계산하지 않고 **자격·급여(給與)·검진** 쪽을 맡는다.
> 퇴직 후 보험료 선택은 가이드에서 다루되 계산은 퇴사노트로 보낸다.
> 의료비 세액공제는 **세금노트** 몫이다.

## 스택·명령

- Next.js 16.2.10 (App Router) + TypeScript + Tailwind CSS 4. DB·로그인·결제·API 없음, 전부 정적.
- 개발 서버: 워크스페이스 `.claude/launch.json`의 `health-note-dev` (포트 4700)
- 빌드: `npm run build` / 테스트: `npm test` (vitest 72개)
- Node는 포터블: 명령 앞에 `$env:Path = "E:\클로드\tools\node;$env:Path"` 필요
- **빌드 성공 판정은 `$LASTEXITCODE -eq 0`으로 한다.**
- **PowerShell `Set-Content -Encoding utf8`은 BOM을 붙인다.** `package.json`에 BOM이
  들어가면 빌드가 통째로 깨진다. `[System.IO.File]::WriteAllText($f, $t, (New-Object System.Text.UTF8Encoding $false))`를 쓸 것.
- 배포: `git push origin main` (Vercel 자동 배포)만 사용. 절차는 `DEPLOY.md`
- 도메인: health.lifebanjang.com
- 테마: 청록(`--accent: #0f7b8a`) — 기존 17개 노트와 겹치지 않음

## 구조

- 계산 엔진 + 테스트 (로직 수정 시 반드시 테스트 함께 갱신):
  - `lib/dependent.ts` 피부양자 자격 — 부양·소득·재산 세 요건을 각각 판정.
    `effectiveIncomeLimit()`이 "재산이 있으면 소득 문턱이 절반"임을 보여준다.
    `formatMoney()`로 억/만 단위 표기를 통일한다.
  - `lib/copay-cap.ts` 본인부담상한제 — 2026년 상한액 7구간 + 요양병원 120일 초과.
    **비급여를 계산에서 제외**하고 `uncoveredRatio`로 그 비중을 드러내는 것이 요점.
  - `lib/checkup.ts` 건강검진 — 출생연도 홀짝 + 암검진 6종의 나이·주기·성별·고위험군.
  - `lib/copay-rate.ts` 본인부담률 — 종별 30/40/50/60%, 입원 20%, 산정특례 5~10%.
    `effectiveRate`가 비급여 때문에 명목 부담률이 무너지는 것을 보여준다.
- 가이드 10편: `lib/guides-1.ts`(자격·보험료 5편) · `lib/guides-2.ts`(병원비·검진 5편)
- 계산기 페이지: `app/calc/{dependent,cap,checkup,rate}/page.tsx`

## 이 노트가 반드시 설명해야 하는 것

### ① 비급여는 어디에도 안 걸린다 — 전 계산기의 공통 논지
비급여는 본인부담률과 무관하게 **전액 본인 부담**이고, **본인부담상한제로도
돌려받지 못하며**, **산정특례로도 줄지 않는다.** "30%만 내면 되는 줄 알았는데
90%를 냈다", "500만원 냈는데 왜 환급이 없나"가 전부 여기서 나온다.
세 계산기(`cap`·`rate`·가이드)에서 반복해 설명한다. **이 설명을 빼지 말 것.**

### ② 피부양자는 '초과하면 즉시' 탈락
소득 2,000만원은 구간별 감액이 아니라 통과/탈락이다. 그리고 **사업자등록이
있으면 사업소득 1원에도 탈락**한다(소득 2,000만원 기준과 별개로 작동).
퇴직 후 프리랜서 사업자등록을 내는 순간 자격을 잃는 경우가 많다.

### ③ 자궁경부암 검진은 만 20세부터
6종 중 가장 이른데 "검진은 중년부터"라는 인식 때문에 가장 많이 놓친다.

## 지역가입자 보험료는 계산하지 않는다 (의도적)

재산을 60등급 점수표로 환산하는 방식이고 점수표와 점수당 금액이 **매년 바뀐다.**
2026-08 조사 시점에 출처마다 점수당 금액이 **195.8 / 208.4 / 211.5원**으로
어긋났고, 등급표 자체를 신뢰할 만한 원문에서 확보하지 못했다.
어설픈 추정은 잘못된 판단을 부르므로 **공단 모의계산으로 안내**한다.
퇴사노트가 지역가입자 보험료를 계산하지 않는 것과 같은 이유다.
**나중에 시행령 별표4를 확보하더라도, 매년 갱신할 수 있을 때만 넣을 것.**

## 주의사항 — 갱신 대상

> 전 노트의 갱신 항목은 워크스페이스 `E:\클로드\CLAUDE.md`의
> **"기준값 갱신 캘린더"** 참조.

- **본인부담상한액** — **매년 1월** 전국소비자물가변동률 반영 고시.
  `lib/copay-cap.ts`의 `CAP_TIERS`. 2026년 일반 90/112/173/326/446/536/843만원,
  요양병원 120일 초과 143/181/245/404/580/698/1,096만원.
  값을 고치면 `copay-cap.test.ts`의 리터럴 고정 테스트가 먼저 깨진다.
- **피부양자 소득·재산 기준** — 시행규칙 개정 시. 2022년 개편에서 소득이
  3,400만원 → 2,000만원으로 내려갔다. 또 내려갈 수 있다.
- **본인부담률·산정특례율** — 시행령 별표2 개정 시.
- **암검진 대상 연령·주기** — 암관리법 시행령 별표1 개정 시.
  대장암 연령 하향 논의가 있으니 확인할 것.
- 값을 고치면 **가이드 본문과 계산기 해설의 숫자**도 함께 훑을 것.

## 검증에 쓸 수 있는 기준값 (테스트에 들어 있음)

- 4~5분위·급여 본인부담 300만원 → 환급 **127만원** (상한 173만원)
- 같은 조건 + 비급여 500만원 → **환급은 그대로 127만원**, 최종 부담 673만원
- 4~5분위·요양병원 121일 → 상한 245만원, 환급 **55만원** (하루 차이로 72만원 더)
- 1980년생 여성·2026년 → 일반검진 대상 + 암검진 **3종**(위·유방·자궁경부)
- 급여 10만원·의원 외래 → **3만원** / 상급종합 → **6만원**
- 급여 10만원 + 비급여 90만원·의원 → 명목 30%인데 **실질 93%**
- 직계존속·소득 1,200만원·재산 2억 → **자격 있음**
- 사업자등록 있음 + 사업소득 1원 → **탈락**

## 그 밖에

- 브라우저 스크린샷은 이 환경에서 타임아웃 가능 — get_page_text/read_page/javascript_tool로 검증.
- PowerShell에서 `app/guide/[slug]` 경로는 `-LiteralPath` 사용. `&&` 불가.
- `components/FamilyLinks.tsx`의 SITES 배열은 전 사이트 공통 정본.
- **가이드를 추가·수정하면** 허브에서 `npm run gen:note-guides`를 돌리고 허브도 함께
  배포해야 `lifebanjang.com/articles/health` 목록에 반영된다.
- 입력한 소득·재산·진료비는 브라우저에서만 처리하고 서버로 보내지 않는다.
- **의료·법률 자문이 아니라는 면책**과 "자격 판정과 환급은 공단이 최종 결정"이라는
  고지를 유지할 것. 건강 관련 주제라 단정적인 표현을 피한다.
